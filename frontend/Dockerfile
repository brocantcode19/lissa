FROM node:18-alpine

WORKDIR /app

# Install dependencies first (Docker layer cache)
COPY package*.json ./
RUN npm install

# Copy source
COPY . .

EXPOSE 3000

# Dev mode with hot reload
CMD ["npm", "run", "dev"]
